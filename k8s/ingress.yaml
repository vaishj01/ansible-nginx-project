apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: wandernest-ingress
  namespace: wandernest

spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix

            backend:
              service:
                name: wandernest-service
                port:
                  number: 80
